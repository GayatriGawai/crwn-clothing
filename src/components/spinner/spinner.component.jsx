import { SpinnerContainer, SpinnerOverlay } from './spinner.styles';

const Spinner = () => {
    console.log('Spinner');
    return (
        <SpinnerContainer>
            <SpinnerOverlay />
        </SpinnerContainer>
    );
};

export default Spinner;
